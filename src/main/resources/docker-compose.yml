services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: straycatz
      POSTGRES_USER: straycatz
      POSTGRES_PASSWORD: straycatz
    ports:
      - "5432:5432"
    volumes:
      - straycatz_pg_data:/var/lib/postgresql/data

volumes:
  straycatz_pg_data:
